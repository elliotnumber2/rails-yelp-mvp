<div class="h1">
  <h1>Add a review for <%= @restaurant.name %></h1>
</div>
<div id="new-resty-form">
  <%= simple_form_for([@restaurant, @review]) do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit %>
  <% end %>
</div>
